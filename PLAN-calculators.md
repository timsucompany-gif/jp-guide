# 日本生活計算器 — 方向分析與開發紀錄

> 狀態：**原型階段**。旅遊分支可以完整走完，三個詳細模組都能用。
> **費率全部未查證**，還不能上線。
> 建立於 2026-09-10，隨開發同步更新。

## 已拍板的決定（2026-09-10）

| 決定 | 內容 | 影響 |
| --- | --- | --- |
| **站點定位** | **純工具站**，不走「我的親身經歷」路線 | 護城河從「有沒有親身經歷」改成「**工具本身難不難做**」。第 4 節評分已依此重算 |
| **網址結構** | 改成 `/jp-guide/calc/xxx/` 分區，並做 `/calc/` 匯總頁 | README 第 1 條規則要改 |
| **租車管道** | 比價平台（Trip）通常最便宜；Orix 直營不錯；**Toyota 偏貴，排除** | 這是 T1 的一塊真實資料，見 6-0 |
| **資料維護** | 可以每季更新 | 含市場價格的題目全部解鎖。SOP 見 8-1 |
| **v1 成功條件** | 「準備自駕的台灣人，**3 分鐘內**得到一個**明顯比自己亂估更可靠**的預算，且**知道 Pass 是否值得買**」 | 這句話直接砍掉一半的功能，見 5-1 |
| **Repo** | **另開新 repo**（你的決定，2026-09-10 覆寫我的建議） | 我的反對理由與後續代價見 7-5 |
| **產品形狀** | 主入口是 wizard（情境 → 基本 → 分類 → 想精算的才點開） | wizard 是黏著劑不是 SEO 入口，見 5-2 |
| **草稿位置** | `_calc-draft/`，`shared/` ＋ wizard ＋ 獨立頁 | 底線開頭 → Jekyll 不部署；另加 noindex 保險 |

## 目前進度（2026-09-10）

**已整理成獨立 repo `jp-tools` 的結構**，`_calc-draft/` 的內容就是該 repo 的根目錄。

    jp-tools/
    ├── index.html              工具總覽（純靜態卡片）
    ├── shared/                 ← 全站共用，改一次到處生效
    │   ├── rates.js  core.js  calc.css
    │   └── rental-module.js  lodging-module.js  rail-module.js
    ├── travel/
    │   ├── trip-cost/          整趟旅費估算（wizard）  ✅
    │   ├── rental-car/         自駕 ＋ Expressway Pass ✅
    │   ├── accommodation/      住宿 ＋ 宿泊稅          ✅
    │   └── jr-pass/            JR Pass 比價            ✅
    ├── living/   finance/      （待做）
    ├── README.md  .nojekyll

原本提案的樹沒有 `shared/`。那是整個結構的重點 ——
沒有它，七個工具會各自複製一份費率與計算邏輯，季更新直接破功。
另外 `-calculator` 後綴在網址裡是重複的（repo 本身就叫 tools），已拿掉。

| 項目 | 狀態 |
| --- | --- |
| wizard 骨架（情境 → 基本 → 分類 → 精算） | ✅ |
| 交通 → 自駕精算（含 Pass 判斷、租車天數獨立於行程天數） | ✅ |
| 住宿 → 精算（宿泊稅、入湯稅、按房／按人、Airbnb 民泊稅務） | ✅ |
| 交通 → 鐵路精算（JR Pass 比較、效期檢查、當地交通分離） | ✅ |
| 跨分類修正（住宿含餐自動折抵餐食） | ✅ |
| 混搭分配（住宿晚數、餐食天數、租車天數各自可分配） | ✅ |
| 每個分類的「其他」自由輸入（自填金額不灌大誤差） | ✅ |
| 信心區間與「還能精算幾項」 | ✅ |
| 幣別切換（日圓／台幣），內部以日圓為準 | ✅ |
| 占比圖（長條同時表達占比與可靠度，可直接點進精算） | ✅ |
| 數字滾動動畫（只動被改到的那組，1.5 秒封頂） | ✅ |
| 即時匯率（jsDelivr ＋ 備援，抓不到退回預設值） | ✅ |
| 工具總覽首頁 ＋ README | ✅ |
| 短期停留／長期居住分支 | ⬜ 顯示但停用 |
| **租車費率查證** | ⬜ **最大的一塊，也是唯一擋著上線的事** |

---

## 查證紀錄

> 這一節獨立出來，是因為它曾經被我自己洗掉一次 ——
> 重寫「目前進度」時用「從標題切到下一節」整段替換，
> 查證紀錄剛好寫在那個範圍內，一起被蓋掉。
> **紀錄要放在不會被例行更新掃到的地方。**

### 住宿（2026-09-10）

| 項目 | 查到什麼 | 對照原本的假資料 |
| --- | --- | --- |
| **京都市宿泊稅** | 2026-03-01 起五階：`<6,000→200`、`6,000–20,000→400`、`20,000–50,000→1,000`、`50,000–100,000→4,000`、`≥100,000→10,000`。按人按晚，基礎是不含餐費與消費稅的住宿費 | ❌ **每一階都低估**，還漏了 50,000–100,000 那階。我記的是舊制（三階、上限 1,000） |
| **東京都宿泊稅** | 現行 `10,000–15,000→100`、`≥15,000→200`，未滿 10,000 免稅 | ✅ 猜對 |
| **東京都 2027 改制** | 2026-07-01 公布條例，**2027-04-01 起改一律 3%、免稅點 13,000 円，並把簡易宿所與民泊納入課稅** | 🆕 原本完全不知道 |
| **民泊課稅** | 因自治體而異。大阪、京都**課**；東京目前**不課** | 🆕 原本一律當成課稅，Airbnb 在東京會憑空多一筆 |
| **Airbnb 行情** | 東京 8,000–12,000／晚、大阪 6,000–9,000、京都個室 7,000–11,000、町家一棟 20,000+ | 🆕 |
| **Airbnb 清潔費** | 套房 3,000–5,000、2LDK 6,000–8,000，一次性 | 🆕 短住時殺傷力最大 |
| **Airbnb 服務費** | ⚠️ **過渡期**：2025 年底起推行房東單一負擔 15.5%、旅客看到即最終價；但官方頁面顯示舊的分離模式（旅客 14.1–16.5%）仍存在 | 做成預設 0＋可自填 |
| **商務旅館** | 主要都市單人房 7,000–15,000／晚 | 🆕 |

### 鐵路票價（2026-09-10 ～ 09-11）

普通車指定席、通常期、運賃＋特急料金合計。**原本的 10 筆有 5 筆錯，而且全部偏低。**

| 區間 | 我編的 | 查證後 |
| --- | --: | --: |
| 成田⇄東京 ／ 關空⇄大阪 ／ 東京→京都 ／ 東京→新大阪 ／ 博多→鹿児島中央 | — | ✅ 五筆猜對 |
| 東京 → 仙台 | 11,410 | **11,630** |
| 東京 → 金沢 | 14,380 | **14,600** |
| 札幌 → 函館 | 9,440 | **9,770** |
| 東京 → 博多 | 23,390 | **23,810** |
| 新大阪 → 廣島 | 10,440 | **10,950** |
| 🆕 東京 → 長野 | — | **8,450** |
| 🆕 東京 → 名古屋 | — | **11,300** |

**區內移動（2026-09-11 補）** —— 原本整張表只有跨區新幹線，
等於假設每個人都要坐新幹線，而且預設還幫關西行程勾了「東京→新大阪」。

| 區間 | 票價 |
| --- | --: |
| 大阪 ⇄ 京都（新快速） | **580** |
| 大阪 ⇄ 三ノ宮 | **420** |
| 京都 ⇄ 奈良（みやこ路快速） | **720** |
| 大阪 ⇄ 奈良（大和路快速） | **820** |
| 東京 ⇄ 橫濱 | **530** |
| 羽田 ⇄ 浜松町（單軌） | **520** |

> 區內移動與跨區長程差了一個數量級（420～820 vs 14,720），
> 這就是「Pass 對單一地區行程幾乎不可能回本」的數字依據。
> 實測關西 5 天 2 人：車票 ¥11,720，最便宜的關西廣域 Pass 要 ¥24,000。

### 當地交通參考價（2026-09-10）

長野電鉄 長野→湯田中 **1,660**／單程；長電巴士 長野→猴子公園 **1,160**；
長野→白馬 **3,500**；觀光纜車來回 **2,000–3,000**。

### 這幾輪查證的教訓

1. **錯的數字有方向性。** 宿泊稅我一律低估（記到舊制），鐵路票價也一律偏低（記到調價前）。
   憑印象的資料不是隨機誤差，是系統性偏舊。
2. **一半對一半錯最危險。** 鐵路 10 筆有 5 筆剛好猜對 ——
   但你分不出是哪五筆，所以「大致上還行」不能拿來背書。
3. **模型錯的時候，查再多數字都沒用。** 住宿原本用「每人每晚 × 人數」算飯店，
   那是錯的模型；使用者一句「APA 一晚 11,000」才點破那是**一間**的價。

### 仍未查證

租車全部（高速費率、Pass 價格、油耗、汽油單價、CDW／NOC、乗り捨て、停車行情）、
JR Pass 各方案價格與涵蓋範圍、大阪／札幌／福岡／金沢的宿泊稅級距、
餐食與每日當地交通行情。

> ⚠️ JR東日本 2026-03 調整過首都圈運賃，關東在來線票價要優先重查。

---

## 0. 先講結論

1. **方向本身成立**，而且比再多寫幾篇圖文攻略更有長期價值。
2. **但「三大類一起開」是錯的。** 現在站上只有 4 篇文章、AdSense 剛起步、網域權重接近零。
   同時開旅遊／短居／長居三條線，結果會是三條都不夠深，一條都排不上去。
3. **建議先只做一支旗艦：日本自駕總成本計算器。** 理由在第 5 節。
4. 最關鍵的認知修正在第 2 節：**單純的「加總 ÷ 人數」沒有價值**，
   任何人打開手機計算機都能算。價值在「使用者算不出來的那部分」。
5. 定位確定是純工具站之後，**最大的風險從「內容不夠獨特」變成「資料會過期」** —
   工具站沒有故事可以撐，數字錯了就什麼都沒了。見第 8 節與第 9 節第 1 題。

---

## 1. 現況盤點（決定了我們能做什麼）

| 項目 | 現況 | 對計算器的影響 |
| --- | --- | --- |
| 技術棧 | 純靜態 HTML／CSS／JS，零相依，GitHub Pages | **沒有後端**。需要即時資料（匯率、油價）的計算器要另想辦法 |
| 內容量 | 4 篇文章（退稅、Visit Japan Web、史努比 ×2） | 權重極低，禁不起主題發散 |
| 原站定位 | `JP Guide｜日本旅遊實用資訊` | 原本是**純旅遊站**。加「長期居住」等於改變站點身分 |
| 既有計算器 | `tax-refund/` 內已有退稅試算 ＋ 90 天期限計算 | **模式已驗證可行**，直接沿用寫法即可 |
| CSS | 每頁內嵌，文章頁各約 355 行、高度重複 | 再加 3～5 頁就會痛。見 7-2 |
| 變現 | AdSense 已接、GA4 已裝 | 有數據可驗證假設，不用憑感覺猜 |
| 站點定位 | **純工具站**，不走個人故事路線（已確認） | 護城河改成「工具本身難不難做」，不是「有沒有親身經歷」 |
| 手上的實料 | 租車比價管道的實際經驗（Trip／Orix／Toyota 的價差） | 可以變成 T1 的一塊硬資料，但要用「行情對照」的形式呈現，不是遊記 |

---

## 2. 這個方向的真正價值在哪（以及最大的陷阱）

### 陷阱：把計算器做成「加法機」

你原本舉的例子：

    租車費 ¥18,000 ＋ ETC ¥4,000 ＋ 高速 ¥6,500 ＋ 油錢 ¥5,000 = ¥33,500 ／ 3 人 = ¥11,167

這個計算**本身沒有價值** — 使用者要能填進那四個欄位，代表他早就知道答案了。
而且純計算器頁面在 SEO 上是重災區：

- 內容單薄，會被 helpful content 系統判為低品質
- 停留時間極短（算完就走），行為訊號差
- AdSense 收益差（沒有閱讀行為＝沒有曝光機會）

### 真正的價值：**使用者填不出來的那一格**

同一個租車計算器，價值全在使用者「填不出來」的欄位：

| 使用者填得出來 | 使用者填不出來 ← **價值在這** |
| --- | --- |
| 天數、人數 | 這段路的高速費到底多少 |
| 租車報價 ¥18,000 | 免責補償（CDW）＋ NOC 要不要保、加多少 |
| — | 乗り捨て（甲地租乙地還）加收多少 |
| — | 軽自動車 vs 普通車，油錢差多少 |
| — | **這趟該不該買 Expressway Pass** |
| — | 都市區停車費（東京／大阪一晚可能吃掉整天預算） |
| — | 深夜／假日 ETC 折扣能省多少 |

**「該不該買 Expressway Pass」這一題，本身就足以撐起一支計算器。**
它是真實的、有明確金額差距的、手算很煩的決策。這才是別人抄不走的東西。

### 原則

> 不做「計算器網站」，做**「有計算器的文章」**。
> 計算器負責吸引點擊與分享，文章負責排名與廣告曝光。
> 現有的 `tax-refund/` 就是正確示範 — 計算器擺在長文後段，不是全部。

---

## 3. 分類架構：三分法要修正

### 你提的分法

    短期居住 ／ 長期居住 ／ 旅遊

**優點**：符合使用者的自我認同（「我是遊客」／「我要待幾個月」／「我要住下來」），
拿來當網站導覽的分區是好的。

**問題**：

1. **「短期居住」定義模糊。** 打工度假？語言學校？出差三個月？長期旅行 30 天？
   這四種人要的工具完全不同。這一格不收斂的話，會變成雜物櫃。
2. **「長期居住」和「旅遊」實質上是兩個網站。**
   受眾不重疊、關鍵字不重疊、廣告主不重疊、內容調性也不重疊。
   現在同時做，等於用一個零權重的網域打兩場仗。
3. 分類是**給人看的**，不是拿來排開發順序的。開發順序要用別的軸。

### 建議的修正

**資訊架構（網站怎麼分區）** — 沿用三分法，但把「短期居住」收斂成一種明確的人：

    旅遊       — 7 天以內，觀光客
    短期停留   — 打工度假／語言學校（1 個月～1 年），有簽證、有收入、要租房
    長期居住   — 工作簽／永住，要繳稅、繳年金、養車

**開發順序（先做哪個）** — 改用這條軸：

    搜尋量 × 我有沒有真實數據 × 競爭低不低 × 資料會不會過期

三分法決定「未來長什麼樣」，這條軸決定「這個月做什麼」。兩件事不要混在一起。

---

## 4. 候選清單與評分

評分 1～5，越高越好（「維護」欄＝**維護越輕鬆分越高**）。

因為定位確定是**純工具站**，原本的「經驗」欄已經換成「**護城河**」：

> **護城河** ＝ 這支計算器需不需要整理難取得的資料、或處理複雜的規則？
> 越難做，越不會被別人五分鐘抄走，工具本身就是差異化。
> 一張表就查得完的東西（例如免稅額度）護城河低 — 不是不能做，是不能當旗艦。

「YMYL」欄標記的是涉及稅務、年金、保險的題目：Google 對這類內容的信任門檻特別高，
而且**算錯的後果是使用者真的少拿到錢**。純工具站沒有專業背書，做這類題目要格外保守。

### 旅遊

| # | 計算器 | 搜尋量 | 競爭低 | 護城河 | 決策價值 | 維護 | 總分 | YMYL |
| --- | --- | :-: | :-: | :-: | :-: | :-: | :-: | :-: |
| T1 | **自駕總成本 ＋ Expressway Pass 該不該買** | 4 | 5 | 5 | 5 | 3 | **22** | — |
| T2 | 台灣入境免稅額試算（額度＋菸酒限量） | 4 | 5 | 2 | 4 | 4 | **19** | — |
| T4 | JR Pass 值不值得買 | 5 | 1 | 4 | 5 | 3 | **18** | — |
| T3 | 宿泊稅／入湯稅試算（京都新制、東京、大阪、福岡） | 3 | 5 | 3 | 3 | 3 | **17** | — |
| T6 | 換匯比較（現金／刷卡／海外提款手續費） | 5 | 2 | 3 | 3 | 1 | **14** | — |
| T5 | 日本旅遊總預算（天數 × 人數 × 等級） | 5 | 1 | 1 | 2 | 2 | **11** | — |
| ✅ | 退稅試算 | — | — | — | — | — | 已完成 | — |

### 短期停留（打工度假／語言學校）

| # | 計算器 | 搜尋量 | 競爭低 | 護城河 | 決策價值 | 維護 | 總分 | YMYL |
| --- | --- | :-: | :-: | :-: | :-: | :-: | :-: | :-: |
| S2 | **租屋初期費用試算**（敷金・礼金・仲介・保証会社） | 3 | 5 | 3 | 5 | 4 | **20** | — |
| S1 | 打工度假手取り試算（時給 × 工時 → 扣稅保後實拿） | 3 | 5 | 4 | 5 | 3 | **20** | ⚠️ |
| S3 | 都道府県最低賃金對照（每年 10 月更新＝常青流量） | 3 | 4 | 2 | 3 | 3 | **15** | — |
| S4 | 語言學校總費用（學費＋住宿＋生活費＋簽證） | 3 | 3 | 2 | 4 | 3 | **15** | — |

### 長期居住

| # | 計算器 | 搜尋量 | 競爭低 | 護城河 | 決策價值 | 維護 | 總分 | YMYL |
| --- | --- | :-: | :-: | :-: | :-: | :-: | :-: | :-: |
| L1 | **年金脫退一時金試算** | 2 | 5 | 5 | 5 | 5 | **22** | ⚠️ |
| L2 | 手取り計算機（年収 → 所得税＋住民税＋社会保険料） | 3 | 4 | 5 | 4 | 2 | **18** | ⚠️ |
| L5 | 永住／歸化在留年數試算 | 2 | 4 | 2 | 3 | 5 | **16** | — |
| L4 | 養車成本（車検・自動車税・重量税・保險・停車位） | 2 | 4 | 3 | 3 | 3 | **15** | — |
| L3 | 日→台匯款手續費比較 | 3 | 3 | 2 | 3 | 2 | **13** | — |

### 重算後的兩個發現

1. **L1 年金脫退一時金跟 T1 並列最高分（22）。** 換成純工具的評分標準後它衝上來了 —
   規則複雜、幾乎零競爭、資料極穩定（幾乎不用維護）、決策金額大。
   **但它是 YMYL，而且算錯會害人少領錢。** 不建議當第一支，但值得排進前三。
2. **「長期居住」這一格建議先只放非稅務題目**（L4 養車、L5 在留年數）。
   稅務／年金／保險類（L1、L2、S1）等站有一定權重、且流程走過一次驗證無誤後再上。

> ⚠️ 上表的「搜尋量」「競爭低」是**我的估計，不是查證過的數據**。
> 開工前應該用關鍵字工具實際驗一次，尤其是 T1、T4、L1。

---

## 5. 建議的開發順序

### 第一支：T1 日本自駕總成本計算器

即使拿掉「親身經歷」這個加分項，T1 還是第一，理由變成純工具邏輯：

1. **護城河最高** — 要整理高速費率、Pass 方案、各車種油耗、都市停車行情、CDW／NOC 費率。
   資料整理量大到別人懶得抄，而且抄了也會過期。這就是差異化。
2. **中文市場競爭極低** — 現有中文文章絕大多數只寫「怎麼租」，不寫「總共要多少錢」。
3. **決策金額大** — Expressway Pass 買錯／該買沒買，一趟差好幾千日圓，使用者有動機分享。
4. **不是 YMYL** — 算錯了頂多預算抓不準，不會害人少領錢或報錯稅。純工具站最適合從這裡開始。
5. **能跟現有文章互相導流** — 自駕的人一定會買東西（→ 退稅）、一定要入境（→ Visit Japan Web）。
6. **維護成本可控** — 高速費率、Pass 方案一年變動一兩次，不像匯率天天跳。

### 第二支：T4 JR Pass 值不值得買

原本因為「沒有親身經歷」而被壓低的題目，在純工具定位下反而合理：

- **搜尋量是全表最高的一批**，而且是明確的購買決策字（「JR Pass 值得嗎」「JR Pass 划不划算」）
- 護城河比想像中高：要建各區間票價表，比對 Pass 售價，這是資料工程不是寫作
- 競爭高，但現有中文內容多半停在「Pass 有哪幾種」，很少真的幫人算
- **跟 T1 天然互補** — 同一個使用者在問「這趟該自駕還是搭鐵路」，
  兩支計算器可以互相導流，甚至之後合併成一支「日本境內交通方式比較」

（替代方案：如果 T4 的票價資料量評估後太大，改做 **T2 台灣入境免稅額試算** —
護城河低但搜尋量不錯、維護輕鬆，適合當快速補位。）

### 第三支：等前兩支的 GA4 數據說話

不要現在排。三個月後看實際曝光、點擊、停留時間，比現在猜準得多。
**L1 年金脫退一時金**放在候補第一順位 — 分數最高，但要等站有權重、且流程能查證無誤再上。

---

## 5-1. v1 成功條件 → 範圍怎麼砍

> **成功條件（已拍板）**
> 一個準備自駕的台灣人，**3 分鐘內**可以得到一個**明顯比自己亂估更可靠**的預算，
> 而且**知道 Pass 是否值得買**。

這句話比「日本所有自駕情境都能算」有用太多，因為它可以被翻譯成設計約束：

| 成功條件的片段 | 翻譯成設計約束 |
| --- | --- |
| 「3 分鐘內」 | **輸入欄位上限 6 個**，而且預設值要好到「什麼都不改就有用」。使用者第一眼看到的必須是一個已經算好的合理情境，不是空白表單 |
| 「明顯比自己亂估更可靠」 | 見下方的操作型定義 |
| 「知道 Pass 是否值得買」 | 計算器 B 必留，而且要輸出**一句明確結論**（「這趟不用買，省 ¥0」／「建議買，省 ¥4,200」），不能只丟兩個數字讓使用者自己比 |

### 「比亂估可靠」的操作型定義

台灣人自己估日本自駕預算時，會算的是**租車費＋油錢**，然後就沒了。
所以 v1 的全部價值就是**幫他補上他一定會漏掉的四筆錢**：

    ① 免責補償 CDW ＋ NOC        ← 幾乎所有人都忘記
    ② 乗り捨て料金（異地還車）   ← 忘記的話一次爆好幾千
    ③ 停車費                     ← 都市住宿時是隱形殺手
    ④ 高速費                     ← 會算的人也常低估

**這四筆 ＋ Pass 那一題，就是 v1 的全部。** 其他都是 v2。

也因此，**逐項拆解清單是核心功能，不是裝飾** —
使用者要看到「啊我真的忘了停車費」，才會相信這個工具比他自己估的準。
（長條圖是裝飾，可有可無；純 CSS 做就好，不要為它引入圖表庫。）

### v1 範圍表

| 進 v1 ✅ | 延到 v2 ✂️ |
| --- | --- |
| 6 個輸入欄位（見 6 節，已縮減） | 常見路線選單（北海道環道、九州縱貫…） |
| 租車費預設值 ＋ 可覆寫 | 三管道完整報價表（v1 只給一句價差提示） |
| CDW／NOC | 深夜割／休日割選項（v1 用平均值） |
| 乗り捨て料金 | 多都市停車費資料庫（v1 只分都市／郊區） |
| 停車費（都市／郊區二分） | 分享用的 query string 網址 |
| 高速費 | 車種細分（v1 只給 軽／普通／休旅 三選） |
| 油錢 | 「租車 vs JR」的跨模式比較 |
| 輸出：總額／每人／台幣／**逐項拆解清單** | 長條圖 |
| **計算器 B：Pass 買不買（明確結論句）** | |

> 砍掉的東西不是不好，是**不影響成功條件**。
> v1 的目標是「上線並開始收數據」，不是「一次做完」。

---

## 5-2. 產品形狀：wizard ＋ 獨立頁的兩層架構

> **決定（2026-09-10）**：主入口是一個 wizard —— 先選情境，再選地區／天數／人數，
> 然後每個分類先問一兩題給粗估，想算準的才點「精算」展開。

### ⚠️ 先講定位，不然會打錯仗

wizard 對應的關鍵字是「日本旅遊費用」，那是第 4 節評分表裡**最低分的題目（11 分）**：
搜尋量最大、競爭最兇、護城河最低。如果把 wizard 當主要 SEO 入口，等於一開站就去打最難的字。

正確的分層是：

    SEO 入口 = 各個獨立詳細頁（/calc/rental-car/、/calc/jr-pass/…）  ← 各吃各的精準字
    產品體驗 = wizard（/calc/）                                      ← 把人留住、串起來

**wizard 不是入口，是黏著劑。** 搜尋來的人落在租車頁，算完後
「要不要順便估整趟？」→ 進 wizard。wizard 本身不指望排名。

### 但架構要現在就定

如果先做五個獨立計算器再回頭合併，會發現它們湊不起來 ——
每個都自己問一次天數、人數、匯率、地區，假設各自為政。
**共用狀態模型現在定，可以省掉之後的整套重寫。** 這是提早做 wizard 骨架唯一但充分的理由。

所以草稿已經拆成「一份程式碼、兩個入口」：

    _calc-draft/
      shared/
        rates.js          費率資料      → 未來的 data/*.json
        core.js           純計算函式    → 未來的 assets/calc.js（零 DOM 操作）
        calc.css          共用樣式      → 未來的 assets/calc.css
        rental-module.js  租車模組      → standalone:true  給獨立頁
                                          standalone:false 給 wizard（不重複問天數人數）
      rental-car/index.html   獨立頁（SEO 入口）
      index.html              wizard

> 註：`file://` 下 `fetch()` 讀 JSON 會被 CORS 擋，但 `<script src>` 可以 ——
> 所以資料檔做成 `.js` 而非 `.json`，雙擊仍能開。

### 三個設計決定

**① 簡單模式和精算模式用同一個計算模型。**

點開精算**不會讓數字跳掉**。你換到的是誤差從 ±40% 降到 ±10%，以及調參數的能力。
這比「簡單模式隨便給一個數、精算才算真的」誠實得多 ——
後者等於承認簡單模式在唬人，而使用者多半只會看簡單模式。

**② 每一格都顯示信心區間，總結顯示加權後的整體誤差。**

wizard 的總數會被**最不準的那一格**拖累，但使用者只會記得總數。
把不確定性攤開（「±39%，還有 1 項可以精算，點開能縮到 ±27%」），
一方面是誠實，一方面讓「點開精算」這件事有明確回報。

**③ 有些格子刻意不做精算。**

餐費完全取決於個人，我們沒有任何比使用者自己更準的資訊。
硬做一個「精算」只會製造假的精確感。所以餐食那格的開關直接標成「不精算」，
並寫清楚理由，誤差誠實留在 ±35%。

**④ 分類之間會互相修正。**

住宿選「一泊二食」時，餐食會自動折抵（¥5,000 ／人／日 × 人數 × 晚數 × 65%），
並在明細上標出扣了多少。訂了含餐方案卻還照常編餐費，是把同一頓飯算兩次。

> 這種跨分類修正是 **wizard 唯一做得到、獨立計算器做不到**的事，
> 也是它作為「黏著劑」真正的價值所在 —— 不是把幾個數字加起來而已。
> 之後每加一個分類，都要問一次：它跟既有分類有沒有重疊或互斥？

> 這是第 2 節那條原則在 wizard 層級的應用：
> **每一格都要達到「比使用者自己估更可靠」才准放進來。**
> 寧可少一個分類，也不要放一個「隨便抓平均」的格子 —— 那會把整個總數的可信度拉低。

### 分類准入標準

| 分類 | 我們算得比使用者準嗎 | 結論 |
| --- | --- | --- |
| 交通（自駕） | ✅ 保險、高速、停車、還車費他都會漏 | 進，精算模組 ✅ |
| 住宿 | ✅ 宿泊稅、入湯稅、服務費，還有按人／按房計價 | 進，精算模組 ✅ |
| 交通（鐵路） | ✅ 票價與 Pass 比較很煩 | 進，精算模組待做 |
| 餐食 | ❌ 完全看個人 | 進，但**只給粗估、明說不精算** |
| 購物 | ❌ 完全看個人 | **不進**（真要估，退稅計算器已經有了） |

### 第一層的死路要處理

情境選「短期停留／長期居住」目前沒有內容。
草稿的做法是**顯示但停用，標「準備中」** —— 讓使用者知道方向，但不會點進空頁面。
等真的做出來再開。

---

## 6. 旗艦品拆解：T1 自駕總成本計算器

### 6-0 從哪裡租：這一段是意外的重點

你提到的「Trip 通常比較便宜、Orix 不錯、Toyota 偏貴」，**不要當成個人心得寫掉，
要做成計算器的一個輸入維度**：

    租車管道：[ 比價平台 ] [ 直營官網 ] [ 台灣代訂 ]
             ↓
    帶出該管道的日租行情預設值，並顯示三者價差

理由：

- 「日本租車哪裡訂最便宜」本身就是一個獨立的高意圖搜尋字
- 使用者最填不出來的第一格就是「租車費大概多少」，這個維度直接補上
- 三家對照是**可驗證的客觀資料**（同車種同日期抓報價），不是個人偏好，
  適合純工具站的調性，也能定期更新

**注意**：租車比價平台幾乎都有聯盟計畫。如果之後要放推薦連結，
必須明確揭露，而且**不能讓聯盟關係影響計算結果的排序** — 一旦被發現，
整站的工具可信度會歸零。這比多賺那點分潤重要太多。

### 頁面結構（文章為主，計算器為輔）

    /jp-guide/calc/rental-car/

    1. 主視覺 ＋ 一句話結論（「日本自駕 3 天 2 人，總成本約 ¥33,500」）
    2. 費用有哪幾塊（逐項拆解，每一項附官方／業者出處）   ← 這段是排名主力
       2-1 租車費：三種管道行情對照（見 6-0）＋ 車種差多少
       2-2 保險：CDW ＋ NOC，到底要不要保
       2-3 高速費：ETC vs 現金、深夜割／休日割
       2-4 油錢：軽 vs 普通 vs 油電，油耗換算
       2-5 停車費：都市區的隱形殺手
       2-6 乗り捨て料金：最容易爆的一筆
    3. 【廣告版位】
    4. 計算器 A：總成本 ＋ 每人分攤
    5. 計算器 B：Expressway Pass 該不該買   ← 差異化核心
    6. 台灣人專屬：駕照日文譯本怎麼辦
    7. 常見踩雷
    8. FAQ（上結構化資料）
    9. 延伸閱讀 → 退稅 ／ Visit Japan Web

### 計算器的輸入輸出

**輸入**（每一項都要有預設值，不能讓使用者面對空白表單）

    天數 / 人數 / 車種（軽・普通・中型・油電）
    租車管道（比價平台 / 直營官網 / 台灣代訂）← 見 6-0
    取還車：同地 or 甲地租乙地
    每日行駛距離，或直接選常見路線（北海道環道、九州縱貫、沖繩本島…）
    是否走高速 / 主要行駛時段（影響深夜割）
    住宿地區（都市 / 郊區 → 帶出停車費預設值）

**輸出**

    總成本 ¥XX,XXX ／ 每人 ¥XX,XXX ／ 換算新台幣 NT$X,XXX
    逐項拆解長條圖（一眼看出錢花在哪）
    ▸ 該不該買 Expressway Pass：買 ¥XX,XXX vs 不買 ¥XX,XXX → 省 ¥X,XXX
    ▸ 換成軽自動車可省 ¥X,XXX

**加分項**（低成本、高效益）

- 結果寫進網址 query string → 使用者能把試算結果貼給同行朋友
  （canonical 要指回乾淨網址，避免產生重複內容）
- 「複製結果」按鈕 → 貼進 LINE 群組討論，是天然的分享機制

### 需要建的資料表（`data/`，JSON）

| 檔案 | 內容 | 更新頻率 |
| --- | --- | --- |
| `expressway-pass.json` | 各家 Pass 方案、天數、價格、適用區域 | 半年 |
| `fuel.json` | 各車種油耗、汽油單價 | 每季 |
| `parking.json` | 主要都市停車費區間 | 每年 |
| `rental-base.json` | **各管道 × 各車種**日租行情、CDW／NOC 費率 | 每季 |
| `fx.json` | 日圓匯率 | 每日（自動，見 7-1） |

---

## 7. 開工前必須先決定的技術問題

### 7-1 匯率怎麼辦（靜態站沒有後端）

| 方案 | 做法 | 評價 |
| --- | --- | --- |
| A | 讓使用者自己填匯率 | 最簡單，但體驗差、跳出率高 |
| B | 前端直接打免費匯率 API | 有 CORS／額度／服務中斷風險，且拖慢首屏 |
| C | **GitHub Actions 每天抓一次，commit 成 `data/fx.json`** | ✅ **建議**。零成本、無外部相依、離線可用、有版本紀錄 |

方案 C 還有個副作用好處：每天有 commit，網站有「活著」的訊號。

### 7-2 CSS 要不要抽出來（現在正是決策點）

現況：每篇文章內嵌約 355 行 CSS，高度重複。README 自己寫「等十篇以上再考慮抽出」，
**加上計算器系列後會直接跨過那條線。**

建議：**現在就抽，但只抽計算器共用的那部分**：

    assets/calc.css   計算器 UI（欄位、分段選擇器、結果卡、長條圖）
    assets/calc.js    共用邏輯（數字格式化、匯率換算、query string 讀寫、複製結果）

文章本身的排版 CSS 維持內嵌（首屏效能考量，原本的判斷是對的）。
理由：計算器 UI 一定會跨頁重複，而且會反覆微調 — 內嵌等於每次改都要改 N 個檔案。

### 7-3 網址結構 ✅ 已決定：採用分區

    /jp-guide/calc/                  匯總頁（吃「日本 費用 計算」「日本 試算」這類字）
    /jp-guide/calc/rental-car/       T1
    /jp-guide/calc/jr-pass/          T4
    …

**現有 4 篇文章不要搬。** 它們已經送過 Search Console，改網址要做 301，
而 GitHub Pages 沒有伺服器端轉址，只能用 meta refresh，是純扣分。
新規則只套用在計算器系列上。

要同步改的地方：

- README「新增文章的規則」第 1 條 — 補上計算器走 `calc/` 分區
- `index.html` 導覽列新增「計算器」，並在 `#tools` 加一個分區
- `sitemap.xml` 每支計算器一筆，匯總頁 priority 給 0.9

### 7-4 旅遊 ＋ 居住要不要放同一個站

| 方案 | 評價 |
| --- | --- |
| **A. 全放同站，導覽加「計算器」** | ✅ **建議**。權重集中最重要，「日本」這把主題傘夠大 |
| B. 居住類另開新站 | 主題最純，但要重新累積內部連結與導覽入口 |
| C. 同站但 URL 分區 `/calc/travel/`、`/calc/living/` | 折衷，之後真要分家比較好搬 |

現階段選 A 或 C。等單一分區超過 20 頁再認真考慮分家。

### 7-5 要不要另開 repo ＋ GitHub Pages

> **決定：另開新 repo。**（2026-09-10，覆寫我下面的建議）
> 以下保留原本的分析，因為裡面有幾個代價是真的、之後會遇到，先寫著備查。
> 草稿階段先放 `_calc-draft/`，等要搬時整包複製走。

#### 既然要另開，這三件事要記得

1. **`assets/calc.css`／`calc.js` 的共用計畫作廢** — 新 repo 自己一套。
   所以草稿刻意做成**單一檔案、零外部相依**，搬家時複製資料夾就好。
2. **跨 repo 連結只能寫絕對網址**，本機預覽測不到。
   導流連結（自駕 → 退稅 → Visit Japan Web）要另外列一張清單，上線後逐一驗。
3. **兩個 repo 不能綁同一個自訂網域。** 未來若買網域，計算器只能吃子網域
   （`calc.example.com`）— 而子網域在 SEO 上**會**被當成不同網站，
   這時「權重不稀釋」的前提就不成立了。要在買網域前重新想一次。

#### ⚠️ 草稿的技術限制：不能用 fetch 讀 JSON

「雙擊本機開啟」＝ `file://` 協定，**`fetch()` 讀本地 JSON 會被 CORS 擋掉**。
所以草稿把所有費率寫在 HTML 內的 `RATES` 物件裡，沒有 `data/*.json`。

這連帶影響 7-1 的匯率方案：GitHub Actions 產生的 `data/fx.json`
**在本機開啟時一樣讀不到**。之後有兩條路：

- 把它產生成 `data/fx.js`（`window.FX = {...}`），用 `<script src>` 載入 — `file://` 可行
- 或接受匯率只在正式部署時生效，本機用預設值

目前草稿用的是「可編輯的匯率欄位 ＋ 預設值」，兩種方案都相容。

---

#### 以下為原本的分析（建議不另開，未採納）

#### 先釐清現況

實際部署架構是**兩個 repo**：

    timsucompany-gif.github.io  →  https://timsucompany-gif.github.io/          入口頁
    jp-guide                    →  https://timsucompany-gif.github.io/jp-guide/  本站

#### 關鍵事實：另開 repo 不會分散 SEO 權重

GitHub Pages 的 project repo 全部掛在**同一個主機名**底下。
`…github.io/jp-guide/` 和 `…github.io/jp-calc/` 對 Google 而言是**同一個網站**。

所以：

- 「分開會稀釋權重」← **這個直覺是錯的**，不會稀釋
- 「分開能讓計算器有獨立權重」← 也是錯的，得不到

SEO 在這裡不構成理由，**兩邊都不算分**。真正的差別全在工程面。

#### 工程面的三個理由（這才是決定性的）

1. **共用元件跨不了 repo。**
   7-2 的整個計畫是抽出 `assets/calc.css` ＋ `assets/calc.js` 給所有計算器共用。
   兩個 repo 就只能複製兩份，或多做一套 build 流程。改一次 UI 要改兩地方 — 這條就足以否決。
2. **內部連結會變醜且測不到。**
   README 規則 5 明文要求「站內連結一律用相對路徑」，這樣本機預覽才測得到、
   換自訂網域也不用改。跨 repo 只能寫絕對網址，兩個好處同時失去。
   而 T1 的整個導流計畫（自駕 → 退稅 → Visit Japan Web）就是靠這些連結。
3. **匯率的 GitHub Actions 只要設一次。**
   兩個 repo ＝ 兩套 workflow，或是一套加跨 repo 推送權杖。沒有必要。

再加上一個未來的坑：**一個 repo 只能綁一個自訂網域。**
若哪天買了網域，單 repo 直接 CNAME 就好；兩個 repo 就被迫用子網域
（`calc.example.com`），而子網域在 SEO 上**才真的會**被當成不同網站看待。

#### 這不是單向門

之後若計算器區長到超過 20 頁、而且發展出完全不同的受眾，再拆出去也不難
（搬檔案 ＋ 加轉址）。**現在拆的成本 ＞ 以後拆的成本**，所以現在不拆。

#### ⚠️ 順帶發現：robots.txt 和 ads.txt 的位置要確認

這兩個檔案**只有放在網域根目錄才會被讀到**：

| 檔案 | 爬蟲／AdSense 實際讀取的位置 | 本 repo 目前的位置 |
| --- | --- | --- |
| `robots.txt` | `https://timsucompany-gif.github.io/robots.txt` | `/jp-guide/robots.txt` ← **讀不到** |
| `ads.txt` | `https://timsucompany-gif.github.io/ads.txt` | `/jp-guide/ads.txt` ← **讀不到** |

README 自己有寫到 ads.txt 這條規則，但目前的部署路徑不符合。

- `robots.txt` 讀不到影響較小（預設就是全部允許，本來就是想要的），
  損失的是 `Sitemap:` 那一行 — 直接在 Search Console 手動提交 sitemap 即可補上。
- **`ads.txt` 讀不到會影響 AdSense 收益**，這個要處理。

我看不到 `timsucompany-gif.github.io` 那個 repo，**請你確認根目錄有沒有這兩個檔案**。
沒有的話，把 `ads.txt` 複製一份過去（內容完全相同），
`robots.txt` 也放一份並在裡面列出兩個 sitemap 的網址。

---

## 8. 風險與維護

| 風險 | 說明 | 對策 |
| --- | --- | --- |
| **資料過期** | 最低賃金每年調、宿泊稅制度會變、油價／匯率會跳 | 每頁顯示「資料更新日」；**不做你不會維護的題目** |
| **YMYL 審查** | 稅務、年金屬 Your-Money-Your-Life，Google 對 E-E-A-T 要求高 | 每個數字都附官方出處；寫「試算」不寫「建議」；明確免責聲明 |
| **算錯的信任損失** | 計算器算錯一次，整站信任歸零 | 每支都要有一組手算驗證過的測試案例，寫進註解 |
| **主題發散** | 旅遊站突然冒出年金計算器 | 導覽分區明確；長居類文章開頭寫清楚「這篇是給在日本長住的人」 |
| **做太多做不完** | 一次開 15 支，全部半成品 | **一次只做一支，做到能見人為止** |

### 8-1 每季更新 SOP ✅ 已確認可執行

「願意每季更新」如果沒有清單，三個月後一定會忘記要更新什麼、去哪裡查。
所以維護機制本身要先設計好，而不是等資料爛掉才補。

#### 三個設計決定

**① 每個資料檔自帶元資料**，不要靠記憶：

```json
{
  "updated": "2026-09-10",
  "next_review": "2026-12-10",
  "source": "https://…官方頁面",
  "note": "基準查詢條件見 benchmark.md",
  "data": { }
}
```

**② 頁面自動顯示新鮮度，過期會自己承認。**
計算器讀 `updated` 後顯示「資料更新日：2026-09-10」；
若超過 `next_review`，自動跳出一行提示「部分費率可能已調整，請以業者現場報價為準」。

> 這件事的價值不只在誠實 — 它讓「忘記更新」的後果從**使用者被誤導**
> 降級成**使用者被提醒**。是純工具站最重要的一道保險。

**③ 定義「基準查詢條件」（`data/benchmark.md`）。**
這是季更新能不能在一小時內做完的關鍵。租車行情如果每次用不同條件抓，
每季的數字根本不能比，更新就會變成每次兩三小時的苦工，然後就放棄了。

固定下來，例如：

    車種：普通車（1.5L 級距）
    地點：那霸機場 / 新千歲機場 / 福岡機場（固定三個點）
    期間：連續 3 天，取還同地
    訂購時點：出發前 30 天
    管道：比價平台 / Orix 官網 / 台灣代訂（各抓一次）

每季只要照這張表重跑一遍，填新數字即可。

#### 季更新檢查清單

| 資料 | 查哪裡 | 頻率 | 大概要多久 |
| --- | --- | --- | --- |
| 各管道租車行情 | 照 `benchmark.md` 重抓報價 | 每季 | ~30 分 |
| 汽油單價 | 資源エネルギー庁 週次調查 | 每季 | ~5 分 |
| Expressway Pass 方案 | NEXCO 東／中／西 官方頁 | 每季（方案常改、有季節限定） | ~15 分 |
| 高速料金費率 | NEXCO 料金檢索 | 每季確認有無調整 | ~10 分 |
| CDW／NOC 費率 | 業者官網 | 每年 | ~10 分 |
| 都市停車行情 | 主要都市代表性停車場 | 每年 | ~15 分 |
| 匯率 | 前端即時抓（見下） | 即時 | 0 |
| **靜態檔版本號** | 四個頁面的 `?v=YYYYMMDD` 一起改 | 每次改資料 | ~1 分 |
| 宿泊稅級距 | 各自治體官網 | 每季（制度變動頻繁） | ~20 分 |
| Airbnb 行情與清潔費 | 照 benchmark.md 抓同條件房源 | 每季 | ~15 分 |
| **Airbnb 服務費模式** | ⚠️ 過渡期，最該重查的一項 | 每季 | ~5 分 |

#### 已排入行事曆的制度變動

| 日期 | 變動 | 要做什麼 |
| --- | --- | --- |
| **2027-04-01** | 東京都宿泊稅改一律 3%、免稅點 13,000 円、民泊納入課稅 | 到期前要同時支援新舊制，並把 `tokyo.minpaku` 改成 `true` |

> ⚠️ **版本號那一行不能省。** 開發時就實測到瀏覽器會把 `rates.js` 快取住 ——
> 你辛苦更新的費率，使用者那邊看到的還是舊的。
> 所以三個頁面的 `<script src="…?v=20260912">` 必須跟著資料一起改，
> 否則整套季更新等於白做。

#### 匯率不用進季更新清單

已改成前端即時抓，三層 fallback：

    ① 靜態預設值 rates.js        離線、file:// 直接開也能用
    ② jsDelivr currency-api      實測 ~47ms
    ③ open.er-api.com            實測 ~216ms

抓不到就靜靜留在預設值，徽章顯示「預設值」而非「即時」，頁面不會壞。
原本 7-1 規劃的 GitHub Actions 方案不需要了。

> 注意：這些是**中間匯率**，不是使用者實際換得到的價格。
> 現鈔／刷卡／海外提款各有 1～3% 價差 —— 頁面上已經寫清楚，並附台灣銀行牌告連結。

**合計每季約 1 小時。** 這個量是可持續的 — 如果設計成每季要花半天，就一定會斷。

建議做法：在 GitHub 開一個 recurring issue，或直接讓 GitHub Actions
在 `next_review` 到期時自動開 issue 提醒。

### ⚠️ 本文件中所有數字都需要查證

我上面提到的具體數字（最低賃金、汽油價格、宿泊稅金額、敷礼金倍數、
脫退一時金年限與源泉徴収比例、Expressway Pass 價格）
**都是憑印象寫的，開工前必須逐項查官方來源**，並照現有文章的慣例附上出處連結。
特別注意：日本汽油暫定稅率近期有變動，油價相關的數字尤其要重新確認。

---

## 9. 待決問題

### 已回答 ✅

| 問題 | 答案 |
| --- | --- |
| 站點定位 | 純工具站，不走個人故事 |
| 租車素材 | 有實際租車經驗（Trip／Orix／Toyota 的價差），做成 6-0 的管道維度 |
| 網址結構 | 採用 `/calc/xxx/` 分區 |
| **資料維護** | **可以每季更新** → T1 成立，含市場價格的題目全部解鎖。SOP 見 8-1 |

**這一題是整個計畫的門檻，過了。** 原本的備案（改做只含穩定規則的題目）不用啟動。

### 剩下的，我先給建議，你不反對就照這樣走

| 問題 | 建議 | 理由 |
| --- | --- | --- |
| 分潤連結要不要做 | **先不做**，但版面預留位置 | 現在流量為零，分潤收不到錢卻先損失中立性。等有量再談，版面預留就不用重排 |
| T4 票價資料願不願意建 | **等 T1 上線後再評估** | 那是第二支的事，現在決定沒有意義，而且到時會有 GA4 數據可參考 |
| 短期停留先留空？ | **先留空** | 先把旅遊分區做厚。分區可以只有一個入口，但不能三個都半空 |

這三題都不擋 T1，所以**不需要現在回答**，往下做就好。

---

## 10. 下一步

**規劃階段到此結束，沒有東西再擋著 T1 了。** 實作順序：

| # | 動作 | 產出 | 備註 |
| --- | --- | --- | --- |
| 1 | 驗證 T1 的實際搜尋量 | — | 避免整個計畫建立在我的猜測上。**這一步值得先做** |
| 2 | 寫下基準查詢條件 | `data/benchmark.md` | 見 8-1③。先定條件，查資料才不會白工 |
| 3 | **查證所有費率資料** | 一份原始筆記 | **最花時間、且決定成敗的一步**（見下） |
| 4 | 建資料檔 | `data/*.json` | 每個都帶 `updated` / `next_review` / `source` |
| 5 | 抽共用元件 | `assets/calc.css`、`calc.js` | 見 7-2 |
| 6 | 匯率自動化 | GitHub Actions → `data/fx.json` | 見 7-1 |
| 7 | **先寫文章** | `/calc/rental-car/index.html` | 文章才是排名主力，計算器最後接上去 |
| 8 | 接計算器 A、B | 同上 | 每支附一組手算驗證過的測試案例（寫在 JS 註解） |
| 9 | 建匯總頁 | `/calc/index.html` | |
| 10 | 收尾 | `index.html`／`sitemap.xml`／README | 導覽加「計算器」、sitemap 補兩筆、README 改第 1 條規則 |
| 11 | 上線 → 送 Search Console → 觀察 30 天 | — | 再決定第二支 |

### 第 3 步要查的清單

依 5-1 砍過範圍後，v1 真正要查的只有這些：

    v1 必要 ─────────────────────────────────────────
    □ Expressway Pass 各方案：適用區域、天數、價格、外國人限定條件  ← 成功條件明寫，最優先
    □ CDW 免責補償、NOC 的費率與適用範圍                          ← 四筆隱形費用之一
    □ 乗り捨て料金的計價方式（跨區加收級距）                       ← 四筆隱形費用之一
    □ 停車費：都市 / 郊區 各一個代表值（v1 不需要分五個城市）        ← 四筆隱形費用之一
    □ NEXCO 高速料金基本費率（v1 用平均值，不做時段折扣選項）        ← 四筆隱形費用之一
    □ 汽油單價  ← ⚠️ 暫定稅率近期有變動，一定要查最新
    □ 油耗基準：軽 / 普通 / 休旅 三種就好
    □ 租車費預設值一組 ＋ 一句管道價差提示（不用完整三管道表）
    □ 台灣駕照日文譯本的申請方式與有效期  ← 文章要用，非計算器

    延到 v2 ─────────────────────────────────────────
    ✂ ETC 深夜割／休日割的詳細適用條件與折扣幅度
    ✂ 五大都市的停車行情細分
    ✂ 三管道 × 三機場的完整報價表（照 benchmark.md 抓）

每一項都要留下官方來源網址，照現有文章的慣例附在文中。

> 注意：`benchmark.md`（8-1③）v1 還是要先寫，只是**先不用照它抓完整報價表**。
> 先把基準條件定下來，第一次季更新才有東西可以照著跑。
